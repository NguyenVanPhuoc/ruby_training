<div class="page-header">
  <h1><%= team.new_record? ? 'Tạo Team Mới' : 'Chỉnh sửa Team' %></h1>
  <p><%= team.new_record? ? 'Thêm team mới vào hệ thống' : 'Cập nhật thông tin team' %></p>
</div>

<div class="form-card">
  <%= form_with(model: team, local: true) do |form| %>
    <% if team.errors.any? %>
      <div class="alert alert-danger" style="margin-bottom: 24px;">
        <i class="fas fa-exclamation-circle"></i>
        <div>
          <strong><%= pluralize(team.errors.count, "lỗi") %> ngăn không cho lưu:</strong>
          <ul style="margin: 8px 0 0 24px;">
            <% team.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :name, "Tên Team", class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control', placeholder: 'Nhập tên team' %>
    </div>

    <div class="form-group">
      <%= form.label :description, "Mô tả", class: 'form-label' %>
      <%= form.text_area :description, class: 'form-control', rows: 4, placeholder: 'Mô tả về team' %>
      <small style="color: #64748b; font-size: 13px; display: block; margin-top: 4px;">
        Giới thiệu ngắn gọn về team này
      </small>
    </div>

    <div class="form-actions">
      <%= form.submit team.new_record? ? 'Tạo Team' : 'Cập nhật', class: 'btn btn-primary' %>
      <%= link_to 'Hủy', teams_path, class: 'btn btn-secondary' %>
    </div>
  <% end %>
</div>